[pytest]
testpaths = tests
# The API walkthrough in test_api.py builds on itself: a block needs trips,
# which need a pattern, which needs stops. Running in file order is
# intentional, not an accident of collection.
addopts = -p no:randomly -q
